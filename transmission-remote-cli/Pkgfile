# Description: A console client for the BitTorrent client Transmission.
# URL: https://github.com/fagga/transmission-remote-cli
# Maintainer: Joacim Olsen Tangen, joacim at joac dot im
# Depends on: python

name=transmission-remote-cli
version=1.7.0
release=1
source=(https://github.com/fagga/$name/archive/v$version.tar.gz)

build() {
	cd $name-$version

	install -D -m 755 transmission-remote-cli $PKG/usr/bin/transmission-remote-cli
	install -D -m 644 transmission-remote-cli.1 $PKG/usr/man/man1/transmission-remote-cli.1
}
